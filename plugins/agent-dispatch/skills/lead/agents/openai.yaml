interface:
  display_name: "Agent Dispatch"
  short_description: "让 Lead 以可审计 Contract 决定保留或选择派发 Profile"
  default_prompt: "使用 $agent-dispatch:lead 编译本次请求，并由 Lead 决定直接完成还是形成显式 Profile 的派发候选。"
policy:
  allow_implicit_invocation: false
