# Direct-retain example

## Human request

> Rename the provisional outer Contract across the domain docs, Skill, and validator. You already have the whole design context, so do it directly and verify the prototype.

## Lead compilation

- Engagement Contract readiness: `ready_to_act` because the design artifacts are concrete and consistent.
- Work Shape: `Execution`; intent and acceptance criteria are fixed.
- Placement Decision: `retain` because the Lead already holds the design history, while a Child would need the same context reconstructed and the Lead would still repeat semantic verification.

Emit: `Lead gate — readiness: ready_to_act; placement: retain; basis: the Lead already holds the design history and would repeat the Child's semantic verification.`

## Lead work state

- Observed outcome: the canonical term and wire name agree.
- Changes: glossary, ADR, Skill resources, and protocol validator.
- Validation evidence: TypeScript tests, Skill validation, installer checks, and retained prototype demos.
- Material caveat: the Child Dispatch path remains outside this tracer bullet.
- Work-state source: Lead inspection and validation.

## Delivery rule

Evaluate the work state against the Engagement Contract, then report `Outcome`, `Placement`, `Changes`, `Validation evidence`, `Material caveats`, and `Work-state source`. Mark only `reported_to_human`; do not claim that the Human accepted the result.
